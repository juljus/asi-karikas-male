/// @description Display tile coordinates
// You can write your code in this editor

// Displays board coordinates and screen position for each tile
// Makes the board really messy when enabled
/*draw_text(x, y, string(ix) + " " + string(iy) + " " + string(iz) + "\n"
+ string(cubic_to_screen(ix, iy)[0]) + "\n" + string(cubic_to_screen(ix, iy)[1]));